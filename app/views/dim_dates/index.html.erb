<h1>Listing dim_dates</h1>

<table>
  <tr>
  </tr>

<% for dim_date in @dim_dates %>
  <tr>
    <td><%= link_to 'Show', dim_date %></td>
    <td><%= link_to 'Edit', edit_dim_date_path(dim_date) %></td>
    <td><%= link_to 'Destroy', dim_date, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New dim_date', new_dim_date_path %>
